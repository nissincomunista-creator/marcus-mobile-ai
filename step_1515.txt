[{"AllowMultiple":false,"EndLine":689,"ReplacementContent":"    const response = await ai.models.generateContent({\n      model: 'gemini-2.5-flash',\n      contents: promptText,\n    });","StartLine":684,"TargetContent":"    const response = await ai.models.generateContent({\n      model: 'gemini-3.5-flash',\n      contents: promptText,\n    });"},{"AllowMultiple":false,"EndLine":919,"ReplacementContent":"// POST /api/garimpar\napp.post('/api/garimpar', async (req, res) => {\n  if (!store.itbiTransactions || store.itbiTransactions.length === 0) {\n    return res.status(400).json({\n      error: 'Nenhum registro de ITBI cadastrado na base municipal. Por favor, adicione transações de ITBI para usarmos como referência de preço por m² antes de garimpar.'\n    });\n  }\n\n  const isGeminiEnabled = !!process.env.GEMINI_API_KEY;\n  const minedAuctions: AuctionProperty[] = [];\n  const todayStr = new Date().toISOString().split('T')[0]; // e.g. \"2026-05-22\"\n\n  // Get distinct list of neighborhoods in ITBI to filter / ground search queries\n  const itbiNeighborhoods = Array.from(new Set(store.itbiTransactions.map(tx => tx.neighborhood)));\n\n  if (isGeminiEnabled) {\n    try {\n      console.log('Starting real-time Grounded Google Search mining with Gemini 2.5-flash...');\n      const ai = new GoogleGenAI({\n        apiKey: process.env.GEMINI_API_KEY,\n        httpOptions: {\n          headers: {\n            'User-Agent': 'aistudio-build',\n          }\n        }\n      });\n\n      const promptMessage = `\nVocê é um especialista em investimentos imobiliários. Hoje é dia ${todayStr}.\nUse a ferramenta do Google Search (Search Grounding) para encontrar leilões de imóveis ativos (que vão acontecer a partir de hoje, com data de leilão futura, ou seja, maior ou igual a ${todayStr}) no Brasil.\nSelecione oportunidades reais anunciadas nos seguintes portais oficiais:\n- zukerman.com.br\n- megaleiloes.com.br\n- fidalgoleiloes.com.br\n- pestanaleiloes.com.br\n- leilaoimovel.com.br\n\nBusque por lotes localizados 
<truncated 18590 bytes>